apiVersion: v1
kind: ConfigMap
metadata:
  name: grade-submission-api-config
  namespace: grade-submission ## must be in the same namespace as the deployment
data:
  MONGODB_HOST: "mongodb"
  MONGODB_PORT: "27017"
