apiVersion: v1
kind: Secret
metadata:
  name: grade-submission-api-secret
type: Opaque
data: ## NOTE: Values can't be store in plain text, so we need to encode them in base64. You can use the following command to encode your values: echo -n 'your_value' | base64
  MONGODB_USER: "YWRtaW4="
  MONGODB_PASSWORD: "cGFzc3dvcmQxMjM="
stringData: ## NOTE: Values can be stored in plain text, but this is not recommended for production environments. Use this only for testing purposes.
  SAMPLE_USER: "some_values"
  SAMPLE_PASSWORD: "some_values"
